// WARMUP FIXTURE — Dummy Java class used only at Docker image build time to trigger
// the SonarQube Java analyzer download. It is never compiled or deployed in production.
public class WarmupJava {
}
