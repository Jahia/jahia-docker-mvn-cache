<!-- WARMUP FIXTURE — Dummy XML file used only at Docker image build time to trigger
     the SonarQube XML analyzer download. It is never used in production. -->
<config><warmup>true</warmup></config>
