// WARMUP FIXTURE — Dummy TypeScript file used only at Docker image build time to trigger
// the SonarQube TypeScript analyzer download. It is never executed in production.
export const warmupTs = true;
