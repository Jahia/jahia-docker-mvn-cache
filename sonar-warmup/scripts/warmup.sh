#!/usr/bin/env bash
# WARMUP FIXTURE — Dummy shell script used only at Docker image build time to trigger
# the SonarQube shell/bash analyzer download. It is never executed in production.
echo "warmup"
